import React from 'react';

const CatchAllRoutes = ({params}) => {
    console.log(params);
    return (
        <div>
            CatchAllRoutes
        </div>
    );
};

export default CatchAllRoutes;